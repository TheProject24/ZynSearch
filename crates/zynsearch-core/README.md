# zynsearch-core

`zynsearch-core` is the embeddable search engine crate for ZynSearch.

It provides:
- indexing and retrieval primitives
- document ingestion helpers
- storage, ranking, and query pipeline components

The crate is designed to be used directly from Rust applications without running a server process.
